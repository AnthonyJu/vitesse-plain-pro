/**
 * @description 详情标签页名称，用于详情页多开时区分，router.query[TAG_NAME]作为标签拼接名称
 */
export const QUERY_TAG_NAME = 'tag-name'

/**
 * @description J-Form组件使用date-picker时的默认时间范围字段, 用于datetimerange类型的FormItem
 */
export const DATE_TIME_RANGE = 'datetimerange'

/**
 * @description J-Form组件使用date-picker时的默认时间范围字段, 用于datetimerange类型的FormItem
 */
export const DATE_TIME_KEYS: [string, string] = ['startTime', 'endTime']

/**
 * 默认 cesium id
 */
export const DEFAULT_CESIUM_ID = 'cesium-map-container'
